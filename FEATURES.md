# LiteLauncher 功能清单（UI原型参考）

> 最后更新：2026-07-21

---

## 一、核心启动器

### 1.1 搜索

- 全局模糊搜索：输入关键词即时匹配应用、文件、文件夹、网页书签、命令、插件
- 分类筛选：支持按类型过滤（application / folder / file / web / command / plugin）
- 搜索结果数量可配置（5-100条）
- 搜索在独立 Worker 线程运行，主进程不阻塞
- 动态搜索项：路径别名、实时解析项与静态目录条目一并展示

### 1.2 启动/执行

- 打开应用程序（.exe / .lnk）
- 打开文件（任意格式，调用系统默认程序）
- 打开文件夹（资源管理器）
- 打开 URL（默认浏览器）
- 执行插件命令（`command:plugin:<id>?action=...`）
- 使用频率自动排序：根据历史使用频次提升排名

### 1.3 目录扫描与索引

- 自动扫描：开始菜单快捷方式
- 可选扫描：Program Files 目录
- 自定义扫描目录：用户添加任意目录
- 排除目录：指定不扫描的路径
- 结果过滤：包含/排除特定目录的结果
- 后台自动刷新 + 文件系统变更监听（实时更新）
- 手动重建索引命令：`command:reindex`

### 1.4 固定项（Pin）

- 固定/取消固定任意项到首页
- 支持自定义路径固定（文件/文件夹不在扫描目录中也可固定）
- 最多 200 个固定项
- 固定项在索引重建后保留
- 固定项数量可配置显示上限

---

## 二、窗口与系统行为

### 2.1 全局快捷键

- 默认：`Alt+Space` 唤起/隐藏窗口
- 自动回退：`Ctrl+Space` → `Alt+Shift+Space` → `Ctrl+Alt+Space`
- 支持环境变量 `LITELAUNCHER_SHORTCUT` 自定义

### 2.2 窗口行为

- 失焦自动隐藏：窗口失去焦点自动隐藏（文件选择器/下载期间暂停）
- 隐藏时清空：窗口隐藏时自动清空搜索输入
- 关闭 = 隐藏：点击关闭按钮不退出，仅隐藏
- 始终置顶：窗口始终显示在其他窗口之上
- 窗口尺寸预设：`compact`（默认）和 `cashflow`（大窗口用于现金流游戏）

### 2.3 单实例

- 仅允许运行一个实例
- 第二实例启动时唤起已有窗口
- `--replace-instance` 参数可强制替换当前实例

### 2.4 系统托盘

- 托盘图标：显示 "LiteLauncher" 提示
- 单击：切换窗口显示/隐藏
- 双击：显示窗口
- 右键菜单：
  - 显示主界面
  - 退出 LiteLauncher

### 2.5 开机启动

- 可配置 Windows 登录时自动启动
- 设置界面中开关控制

---

## 三、首页布局

### 3.1 搜索栏

- 顶部输入框，支持即时搜索
- 输入为空时显示首页内容

### 3.2 首页内容区域

| 区域 | 说明 | 配置 |
|------|------|------|
| 最近使用 | 按使用频率/时间排列的最近项 | 数量可配（默认20，范围5-100） |
| 固定项 | 用户手动固定的常用项 | 数量可配（默认20，范围5-100） |
| 插件入口 | 当前可见的插件快捷入口 | 数量可配（默认20，范围5-100） |

### 3.3 搜索结果列表

- 匹配项列表，带图标、名称、路径/描述
- 键盘上下选择，回车执行
- 右键/操作按钮：固定、取消固定

---

## 四、设置面板

### 4.1 搜索显示配置

| 配置项 | 说明 | 范围 |
|--------|------|------|
| recentLimit | 首页最近使用显示数量 | 5-100，默认20 |
| pinnedLimit | 首页固定项显示数量 | 5-100，默认20 |
| pluginLimit | 首页插件入口显示数量 | 5-100，默认20 |
| searchLimit | 搜索结果最大显示数量 | 5-100 |

### 4.2 目录扫描配置

| 配置项 | 说明 |
|--------|------|
| scanProgramFiles | 是否扫描 Program Files（开关） |
| customScanDirs | 自定义扫描目录列表（可添加/删除） |
| excludeScanDirs | 排除扫描目录列表 |
| resultIncludeDirs | 结果仅包含这些目录 |
| resultExcludeDirs | 结果排除这些目录 |

### 4.3 插件管理

- 插件可见性开关：逐个切换插件是否显示在启动器中
- 最多管理 50 个插件
- 必选插件（始终显示）：hardware-inspector、clipboard-workbench、webtools-file-hash、webtools-port-helper、webtools-image-prompt、codeagent-switch

### 4.4 系统设置

- 开机启动：开/关
- 全局快捷键显示（当前绑定的快捷键）

### 4.5 应用更新

- 检查更新按钮
- 显示当前版本 / 最新版本
- 查看更新日志
- 安装更新并重启

### 4.6 错误日志

- 查看错误日志列表（按时间倒序）
- 错误分类：main / renderer / IPC / execute / system
- 清除所有日志

---

## 五、剪贴板历史

### 5.1 剪贴板服务

- 后台监听系统剪贴板变化
- 自动记录文本类型的剪贴板内容
- 持久化存储到 SQLite

### 5.2 剪贴板查询

- 搜索历史剪贴板内容
- 按时间倒序展示

### 5.3 操作

- 复制：重新复制某条记录到剪贴板
- 删除：删除单条记录
- 清空：清除所有剪贴板历史

---

## 六、插件详情

### 6.1 富爸爸现金流（cashflow-game）

- 完整现金流桌游模拟器
- 游戏UI使用独立大窗口（cashflow尺寸预设）
- 游戏数据持久化到 SQLite
- 支持游戏存档/读档

### 6.2 硬件检测（hardware-inspector）⭐必选

- 显示系统硬件信息：
  - CPU（型号、核心数、频率）
  - 内存（总量、已用、可用）
  - GPU（型号、显存）
  - 磁盘（容量、已用、可用）
  - 操作系统（版本、架构）
- 指标网格展示
- 导出功能：PNG 截图 / HTML 报告 / Markdown 文本

### 6.3 剪贴板工作台（clipboard-workbench）⭐必选

- 高级剪贴板管理器
- 自动收集：文本内容、图片资源（存储到磁盘）
- 搜索/过滤剪贴板历史
- 后台服务持续运行

### 6.4 截图贴图（litesnap）⭐必选

#### 截图

- 快捷键触发截图（可自定义快捷键）
- 区域框选截图
- 截图保存到磁盘（可配置保存路径）
- 截图历史浏览

#### 贴图

- 将截图/剪贴板图片钉在桌面（Always on top）
- 切换贴图可见性
- 切换贴图点击穿透（click-through）
- 关闭所有贴图
- 贴图快捷键可自定义

#### 取色器

- 快捷键触发取色模式
- 拾取屏幕任意位置的像素颜色
- 显示颜色值

#### OCR 文字识别

- 对截图区域进行文字识别
- 支持语言：中文（zh-CN）、英文（en-US）
- OCR 语言包可下载安装
- 识别结果显示在启动器面板中

#### 截图翻译

- OCR 识别 + 百度翻译流水线
- 对截图区域中的文字进行翻译

#### 配置

- 截图快捷键
- 贴图快捷键
- 取色快捷键
- 穿透切换快捷键
- 保存路径

### 6.5 离线词典（dictionary）

- 英汉离线词典
- 全文搜索（FTS）
- 输入候选词提示
- 查询历史记录
- 收藏夹：收藏单词 + 添加笔记
- TTS 朗读开关
- 收藏导出为 CSV
- 词典数据包可下载安装

### 6.6 文本翻译（webtools-translate）

- 百度翻译 API 集成
- 支持两种引擎：标准翻译 / 大模型翻译（LLM）
- 可配置 API 凭证：
  - appId
  - secret
  - apiKey（大模型引擎）

### 6.7 CodeAgent Switch（codeagent-switch）⭐必选

- 切换编程 AI 助手工具
- 支持的 Agent：Codex、Claude Code、Gemini CLI 等
- 读取/管理 Agent 配置

### 6.8 密码工具（webtools-password）

- 密码生成器
- 可配置参数：
  - 密码长度
  - 生成数量
  - 字符集（大写/小写/数字/特殊字符）
  - 排除相似字符（如 0/O, 1/l）

### 6.9 Cron 生成器（webtools-cron）

- Cron 表达式可视化构建
- Cron 表达式解析为人类可读描述
- 下次运行时间预览（显示接下来N次执行时间）
- 历史记录持久化

### 6.10 JSON 工具（webtools-json）

- JSON 格式化（美化）
- JSON 压缩
- JSON 校验（语法错误高亮，错误位置定位）
- 格式转换：JSON ↔ CSV / YAML
- 字段选择/提取

### 6.11 JSON Schema 校验（webtools-json-schema）

- 输入 JSON 数据 + JSON Schema
- 验证数据是否符合 Schema
- 错误路径报告（精确定位不合规字段）

### 6.12 文本脱敏/假数据（webtools-data-mask）

#### 数据脱敏

- 手机号脱敏（中间四位替换为 *）
- 邮箱脱敏
- 身份证号脱敏

#### 假数据生成

- 姓名
- 邮箱
- 手机号
- UUID
- 公司名

### 6.13 加密工具（webtools-crypto）

#### 哈希计算

- MD5
- SHA-1 / SHA-256 / SHA-512

#### 加解密

- AES 加密/解密（支持 Key + IV 输入）
- RSA 加密/解密
- RSA 密钥对生成（2048位及以上）

### 6.14 JWT 工具（webtools-jwt）

- JWT 解码：输入 Token 查看 Header + Payload
- JWT 编码：构建 Token
- 支持 JWS 和 JWE 格式
- 密钥验证签名

### 6.15 时间戳工具（webtools-timestamp）

- Unix 时间戳 → 人类可读日期时间
- 日期时间 → Unix 时间戳
- 支持秒级/毫秒级时间戳
- 实时时钟显示（当前时间戳实时更新）

### 6.16 正则工具（webtools-regex）

- 正则表达式输入
- 测试文本输入
- 匹配结果高亮显示
- 捕获组展示
- 替换模式预览

### 6.17 字符串工具（webtools-strings）

- Base64 编码/解码
- URL 编码/解码
- Unicode 编码/解码
- 大小写转换（upper / lower / camel / snake / kebab 等）
- 字符统计

### 6.18 颜色工具（webtools-colors）

- 颜色格式互转：HEX ↔ RGB ↔ HSL ↔ HSV 等
- 可视化颜色选择器
- 颜色预览

### 6.19 文本对比（webtools-diff）

- 并排（side-by-side）文本对比
- 差异高亮显示
- 选项：忽略大小写 / 忽略空白字符

### 6.20 HTTP Mock Server（webtools-http-mock）

- 本地 HTTP 模拟服务器
- 配置 Mock 路由和响应
- 用于 API 开发测试

### 6.21 图片 Base64（webtools-image-base64）

- 图片文件 → Base64 字符串
- Base64 字符串 → 图片预览/下载

### 6.22 图片提示词（webtools-image-prompt）⭐必选

- AI 图片生成提示词构建器
- 预设模板/风格选择
- 提示词组合拼接

### 6.23 配置转换（webtools-config-convert）

- 支持格式：JSON / YAML / TOML / INI / Properties
- 任意格式之间互相转换

### 6.24 SQL 格式化（webtools-sql-format）

- SQL 语句美化/格式化
- 支持常见 SQL 方言

### 6.25 单位换算（webtools-unit-convert）

- 长度（米/厘米/英寸/英尺等）
- 重量（千克/磅/盎司等）
- 温度（摄氏/华氏/开尔文）
- 数据大小（B/KB/MB/GB/TB）
- 更多单位类别

### 6.26 文件哈希（webtools-file-hash）⭐必选

- 选择本地文件计算校验和
- 支持算法：MD5 / SHA-1 / SHA-256 / SHA-512 等
- 可用于文件完整性校验

### 6.27 端口助手（webtools-port-helper）⭐必选

- 查询本机端口占用情况
- 显示占用进程信息
- 端口管理操作

### 6.28 URL 解析（webtools-url-parse）

- 输入 URL 分解显示：
  - 协议（Protocol）
  - 主机（Host）
  - 端口（Port）
  - 路径（Path）
  - 查询参数（Query Params）逐个展示
  - 哈希（Hash）

### 6.29 二维码生成（webtools-qrcode）

- 输入文本/URL
- 生成二维码图片
- 可下载/复制

### 6.30 Markdown 预览（webtools-markdown）

- 左侧输入 Markdown 源码
- 右侧实时渲染预览

### 6.31 UA 解析（webtools-ua）

- 输入 User-Agent 字符串
- 解析显示：浏览器名称+版本、操作系统、设备类型

### 6.32 API 调试（webtools-api-client）

- HTTP 请求发送（GET/POST/PUT/DELETE 等）
- 请求头配置
- 请求体编辑
- 响应查看（状态码、Headers、Body）
- 类似迷你 Postman

---

## 七、划词翻译（独立功能）

- 全局快捷键触发：捕获任意应用中当前选中的文字
- 智能路由：
  - 单词/短语 → 离线词典查询
  - 长文本 → 百度在线翻译
- 浮窗显示：在光标附近弹出翻译/词典结果窗口
- 配置项：
  - 启用/禁用
  - 触发快捷键
  - 点击浮窗外部自动关闭
  - 取词后恢复剪贴板内容

---

## 八、系统级功能

### 8.1 应用图标

- 多策略图标提取：
  - .lnk 快捷方式目标解析
  - Squirrel/Update.exe 检测
  - .icns 文件解析
  - Windows ExtractAssociatedIcon（PowerShell 调用）
  - LRU 缓存加速

### 8.2 应用更新

- 启动时自动检查更新
- 手动检查更新
- 下载并安装更新
- 安装后自动重启

### 8.3 错误日志系统

- 错误持久化到 SQLite
- 分类：main / renderer / IPC / execute / system
- 设置面板中查看和清除

### 8.4 开发/测试模式

- `LITELAUNCHER_DEV=1`：渲染进程文件变更自动刷新
- `LITELAUNCHER_E2E=1`：E2E 测试模式，独立 userData 目录
- `LITELAUNCHER_DEBUG_KEYS=1`：键盘事件调试日志

---

## 九、技术约束（原型参考）

- 目标用户：中文用户（所有 UI 文案为中文）
- 平台：Windows（主要）、macOS（兼容）
- 窗口框架：Electron（无边框窗口）
- 无前端框架：原生 DOM 操作，单页面应用
- 插件面板：在主窗口内切换显示，非弹出窗口
- 截图贴图：独立覆盖窗口（Overlay）
- 划词翻译：独立浮动小窗口
