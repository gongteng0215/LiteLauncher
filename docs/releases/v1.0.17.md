# LiteLauncher v1.0.17 Release Notes

Release date: 2026-05-29

## Highlights

- Unified Windows Store application identities across catalog indexing and dynamic command search, so `StartApps / WindowsApps` results keep the same stable `app:startapp:*` item ids whether the PATH alias is available or not.
- Closed the remaining Microsoft Store `Codex` pinning regression path and generalized the fix so the same mismatch does not reappear for similar Store apps.
- Improved pin feedback across the stack: the launcher now reports clearer pin failure reasons, and the settings error log surfaces readable summaries for pin-related failures.

## Quality

- Added regression coverage for catalog/dynamic-search Windows Store id consistency, structured pin result status text, renderer-side readable pin error log formatting, and renderer preference for stable `app:startapp:*` results.
- Verified the targeted release gate with focused build + regression checks instead of a full app-wide smoke sweep.

## Verification

- `pnpm run build`
- `node dist/test/renderer-pinning-status.test.js`
- `node dist/test/renderer-error-log-source.test.js`
- `node dist/test/renderer-startapp-source.test.js`
- `node dist/test/search-section-grid-style.test.js`
- `node dist/test/launcher-main-flow-regression.test.js`
- `node dist/test/windows-app-alias-regression.test.js`
- `node dist/test/e2e-search-layout-smoke.test.js`

## Notes

- GitHub already contains `v1.0.16`, so this release advances the app version and tag to `v1.0.17`.
- Desktop packages continue to publish through the tag-driven GitHub Actions workflow.

**Full Changelog**: https://github.com/gongteng0215/LiteLauncher/compare/v1.0.16...v1.0.17
